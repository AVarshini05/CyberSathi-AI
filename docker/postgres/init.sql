-- Custom PostgreSQL initialization scripts (if any) can be placed here.
-- The database itself is automatically created via the POSTGRES_DB env variable.
SELECT 'CCRMS Database Initialized' as status;
